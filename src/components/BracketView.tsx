import type { Match, Team } from '../types'

// Stub — full implementation in Task 7
export default function BracketView(_props: {
  matches: Match[]
  onAdvance: (matchId: string, winner: Team) => void
}) {
  return <main />
}
